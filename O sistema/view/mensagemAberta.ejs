<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <link rel="icon" href="/img/ico.png" type="image/png">
  <meta charset="UTF-8">
  <title>Mensagem já aberta • QR Mail 42</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css2?family=Comic+Neue:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #d3edff;
      --circle: #60a5fa;
      --face: #1e40af;
      --white: #ffffff;
      --shadow: rgba(0, 0, 0, 0.08);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background-color: var(--bg);
      font-family: 'Comic Neue', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      padding: 1rem;
    }

    .container {
      text-align: center;
      background: var(--white);
      padding: 2.5rem 2rem;
      border-radius: 16px;
      box-shadow: 0 10px 20px var(--shadow);
    }

    .icon {
      width: 80px;
      height: 80px;
      margin: 0 auto 1rem;
    }

    h1 {
      font-size: 1.8rem;
      color: var(--face);
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="icon">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
        <circle cx="50" cy="50" r="48" fill="#60a5fa"/>
        <circle cx="35" cy="40" r="6" fill="#1e40af"/>
        <circle cx="65" cy="40" r="6" fill="#1e40af"/>
        <path d="M35 70 Q50 58 65 70" stroke="#1e40af" stroke-width="5" fill="none" stroke-linecap="round"/>
      </svg>
    </div>
    <h1>Mensagem já aberta :(</h1>
  </div>
</body>
</html>
