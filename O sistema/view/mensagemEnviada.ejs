<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Mensagem Enviada • QR Mail 42</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css2?family=Comic+Neue:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #10b981;
      --primary-dark: #047857;
      --background: #ecfdf5;
      --text: #064e3b;
      --white: #ffffff;
      --border: #a7f3d0;
      --shadow: rgba(0, 0, 0, 0.08);
      --transition: all 0.3s ease;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background-color: var(--background);
      font-family: 'Comic Neue', sans-serif;
      color: var(--text);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      min-height: 100vh;
    }

    .card {
      background: var(--white);
      border: 2px solid var(--primary);
      border-radius: 16px;
      padding: 2.5rem 2rem;
      box-shadow: 0 10px 20px var(--shadow);
      max-width: 600px;
      width: 100%;
      text-align: center;
    }

    .icon-check {
      width: 60px;
      height: 60px;
      background-color: var(--primary);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1rem;
      box-shadow: 0 4px 8px var(--shadow);
    }

    .icon-check svg {
      width: 28px;
      height: 28px;
      fill: #ffffff;
    }

    h1 {
      color: var(--primary);
      font-size: 2rem;
      margin-bottom: 1rem;
    }

    p {
      white-space: pre-line; 
      text-align: justify;
      margin: 0.5rem 0;
      font-size: 1.1rem;
    }

    .code {
      font-weight: 700;
      color: var(--primary-dark);
    }

    .btn {
      display: inline-block;
      margin-top: 1.8rem;
      padding: 0.75rem 1.5rem;
      font-size: 1rem;
      color: var(--white);
      background-color: var(--primary);
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: var(--transition);
      text-decoration: none;
      font-weight: bold;
      box-shadow: 0 2px 6px var(--shadow);
    }

    .btn:hover {
      background-color: var(--primary-dark);
      transform: scale(1.03);
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="icon-check">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
        <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/>
      </svg>
    </div>
    <h1>Mensagem enviada com sucesso!</h1>
    <p><strong>Código do QR:</strong> <span class="code"><%= codigo %></span></p>
    <p><strong>Conteúdo da mensagem:</strong></p>
    <p><%= mensagem %></p>
    <a class="btn" href="https://qrmail42.wtf/v/<%= codigo %>">Ver Mensagem</a>
  </div>
</body>
</html>
